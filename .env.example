DATABASE_URL="postgresql://jetonbro:jetonbro@127.0.0.1:55433/jetonbro"
AUTH_SECRET="replace-with-a-long-random-secret"
AUTH_URL="http://localhost:3000"
EMAIL_FROM="JetonBro <noreply@localhost>"
# Required for real email delivery of magic links and invitations.
# Leave empty locally to write links to tmp/mailbox.jsonl only.
EMAIL_SERVER=""
# Local/CI mailbox and test sessions. Ignored and closed in production.
ALLOW_DEV_MAILBOX="false"
