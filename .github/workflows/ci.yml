name: CI

on:
  push:
  pull_request:

jobs:
  guardrails:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: jetonbro
          POSTGRES_PASSWORD: jetonbro
          POSTGRES_DB: jetonbro_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U jetonbro -d jetonbro_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgres://jetonbro:jetonbro@localhost:5432/jetonbro_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - run: npm ci
      - name: Check route manifest and wallet/ledger writes
        run: bash scripts/check-routes.sh
      - run: npm test
